{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Longboarder list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Username</th>
                <th>Location</th>
                <th>Discipline</th>
                <th>Age</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('DevTRW_Lonboarder_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.username }}</td>
                <td>{{ entity.location }}</td>
                <td>{{ entity.discipline }}</td>
                <td>{{ entity.age }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('DevTRW_Lonboarder_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('DevTRW_Lonboarder_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('DevTRW_Lonboarder_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
